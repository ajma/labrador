services:
  homer:
    image: b4bz/homer:latest
    volumes:
      - ./homer-assets:/www/assets
    ports:
      - "8080:8080"
    environment:
      - UID=1000
      - GID=1000
    restart: unless-stopped
