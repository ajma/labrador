services:
  jellyseerr:
    image: fallenbagel/jellyseerr:latest
    environment:
      - LOG_LEVEL=debug
      - TZ=UTC
    ports:
      - "5055:5055"
    volumes:
      - ./config:/app/config
    restart: unless-stopped
