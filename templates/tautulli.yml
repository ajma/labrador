services:
  tautulli:
    image: tautulli/tautulli:latest
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
    volumes:
      - ./config:/config
    ports:
      - "8181:8181"
    restart: unless-stopped
